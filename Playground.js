var ourArray = [1, 2, 3];
ourArray.push(4); // adding to the end of the array
ourArray.unshift(0); // adding to the beginning of the array

console.log(ourArray);

var number = ourArray.pop(); // removes and returns last element in the array
var otherNumber = ourArray.shift(); // removes and returns the first element in the array


console.log(ourArray);
